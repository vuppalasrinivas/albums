<%= form_for @album, :html => { :class => 'form-horizontal', multipart: true } do |f| %>
  <% if @album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
      <% @album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :placeholder=> "Album Name" %>
  </div>
  <br/>
    <div class="control-group">
    <%= f.label :upload_max_25_photos, :class => 'control-label' %>

    <div class="controls">
      <!-- The magic is coming ...look at my eyes....shazammmm -->
      <%= file_field_tag "images[]", type: :file, multiple: true, size: "2" %>
    </div>
    <br/><br/>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
